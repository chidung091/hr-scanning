<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content="{{ csrfToken }}" />
    <title>
      @section('title')
        Japanese Learning Quiz - Master Hiragana & Katakana
      @end
    </title>

    <link rel="preconnect" href="https://fonts.bunny.net" />
    <link href="https://fonts.bunny.net/css?family=inter:400,500,600,700" rel="stylesheet" />

    <script src="https://cdn.tailwindcss.com">
      
    </script>

    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: [ "Inter", "sans-serif" ]
            },
            colors: {
              primary: {
                DEFAULT: "#3b82f6",
                50: "#eff6ff",
                100: "#dbeafe",
                500: "#3b82f6",
                600: "#2563eb",
                700: "#1d4ed8",
                900: "#1e3a8a"
              }
            }
          }
        }
      };
    </script>

    @vite(['resources/css/app.css', 'resources/js/app.ts'])
    @stack('dumper')
  </head>
  <body class="min-h-screen bg-gray-50 font-sans">
    @include('partials/navigation')

    @section('content')
    @end
    
    @include('partials/footer')

    <!-- Load modular TypeScript application -->
    <script type="module">
      // Import and initialize the main application
      import JapaneseQuizApp from "/resources/js/app.ts";
      // Application will auto-initialize when imported
      
      console.log("Japanese Learning Quiz - Modular Application Loaded");
    </script>
  </body>
</html>
